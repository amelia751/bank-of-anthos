apiVersion: apps/v1
kind: Deployment
metadata:
  name: terms-agent
  namespace: boa-agents
  labels:
    app: terms-agent
    component: ai-agent
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: terms-agent
  template:
    metadata:
      labels:
        app: terms-agent
        component: ai-agent
        tier: backend
    spec:
      containers:
      - name: terms-agent
        image: terms-agent:latest
        imagePullPolicy: Never  # For local development
        ports:
        - containerPort: 8082
        env:
        - name: PORT
          value: "8082"
        - name: GEMINI_API_KEY
          valueFrom:
            secretKeyRef:
              name: boa-agents-secrets
              key: GEMINI_API_KEY
        envFrom:
        - configMapRef:
            name: boa-agents-config
        resources:
          requests:
            memory: "512Mi"
            cpu: "500m"
          limits:
            memory: "1Gi"
            cpu: "1000m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8082
          initialDelaySeconds: 30
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /health
            port: 8082
          initialDelaySeconds: 5
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: terms-agent
  namespace: boa-agents
  labels:
    app: terms-agent
    component: ai-agent
spec:
  selector:
    app: terms-agent
  ports:
  - port: 8082
    targetPort: 8082
    protocol: TCP
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: terms-agent-hpa
  namespace: boa-agents
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: terms-agent
  minReplicas: 2
  maxReplicas: 8
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
